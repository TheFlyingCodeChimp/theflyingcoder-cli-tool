export default `It's simple, just run:

    digi <command>
    
Or even leave out the command and choose from the prompt.

Commands:
    digi create <name> <template>    Creates a new project based off the selected template. 
                                                               You can leave both arguments out and select from the prompts too

Options:
    --version     Shows the version number
    --help          Show this help text
`;